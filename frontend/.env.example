# API base URL for connecting to the backend
# In development with Vite proxy: leave commented out (proxy handles /api -> http://localhost:8000)
# In Docker or direct connection: set to the backend URL
# VITE_API_URL=http://localhost:8000
